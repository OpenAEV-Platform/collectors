services:
  collector-prompt-security:
    image: openaev/collector-prompt-security:latest
    environment:
      - OPENAEV_URL=${OPENAEV_URL}
      - OPENAEV_TOKEN=${OPENAEV_TOKEN}
      - OPENAEV_TENANT_ID=${OPENAEV_TENANT_ID}
      - COLLECTOR_ID=${COLLECTOR_ID}
      - COLLECTOR_NAME=${COLLECTOR_NAME:-"Prompt Security"}
      - COLLECTOR_PLATFORM=${COLLECTOR_PLATFORM:-LLM_FIREWALL}
      - COLLECTOR_LOG_LEVEL=${COLLECTOR_LOG_LEVEL:-warn}
      - COLLECTOR_PERIOD=${COLLECTOR_PERIOD:-PT120S}
      - COLLECTOR_BASE_URL=${COLLECTOR_BASE_URL}
      - COLLECTOR_APP_ID=${COLLECTOR_APP_ID}
      - COLLECTOR_AUTH_HEADER=${COLLECTOR_AUTH_HEADER:-APP-ID}
    restart: unless-stopped

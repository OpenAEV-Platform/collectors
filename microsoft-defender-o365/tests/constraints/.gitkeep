# This directory will hold the UNHAPPY PATH (.feature) acceptance criteria
# for this collector, following BDD conventions.
#
# No unhappy path scenario was defined in PR #491 / issue #471 (chk1 - project
# architecture setup); only happy path scenarios were provided, extracted to
# ../features. This placeholder keeps the directory tracked in git until
# constraint scenarios are added.
